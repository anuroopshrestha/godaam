extends ../layout

block content
  .page-title
    h1 #{title}
  .card-row
    .card
      .card-title All Locations
      if users.length < 1
        p No locations found
      else
        table.striped
          tr
            th S. No.
            th Name
            th
            th Address
            th Products